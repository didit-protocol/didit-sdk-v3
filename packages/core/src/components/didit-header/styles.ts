import { css } from 'lit'

export default css`
  :host {
  }

  .close-button {
    margin-left: auto;
  }
  ui-icon-link[data-hidden='true'] {
    opacity: 0 !important;
    pointer-events: none;
  }
`
