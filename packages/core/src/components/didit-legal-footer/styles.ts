import { css } from 'lit'

export default css`
  ui-flex {
    background-color: var(--ui-color-background);
  }

  a {
    text-decoration: none;
    color: var(--ui-color-soft);
    font-weight: 500;
  }
`
