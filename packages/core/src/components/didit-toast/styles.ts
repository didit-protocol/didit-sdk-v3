import { css } from 'lit'

export default css`
  :host {
    display: block;
    position: fixed;
    opacity: 0;
    pointer-events: none;
    top: 2%;
    left: 50%;
    width: max-content;
    border: white;
  }
`
