// Do a setup test just for checking the test proccess
import { describe, expect, it } from 'vitest'

describe('Test setup', () => {
  it('should pass', () => {
    expect(true).to.equal(true)
  })
})
